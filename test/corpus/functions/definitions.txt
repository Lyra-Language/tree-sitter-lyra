=============================================
Basic Function declaration without parameters
=============================================

let foo = () => {
  // do stuff
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list)
      (block
        (comment)))))

=============================================
Basic Function declaration with visibility
=============================================

pub let foo = () => {
  // do stuff
}

---

(program
  (declaration
    (visibility)
    (identifier)
    (lambda_expr
      (parameter_list)
      (block
        (comment)))))

==============
Async function
==============

let fetchJSON = async (url: string, options: FetchOptions) -> Response => {
  let resp = await fetch(url, options)
  return JSON.parse(resp)
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (async_modifier)
      (parameter_list
        (parameter
          (identifier)
          (string_type))
        (parameter
          (identifier)
          (user_defined_type_name)))
      (return_type
        (user_defined_type_name))
      (block
        (declaration
          (identifier)
          (await_expr
            (call_expr
              (identifier)
              (argument_list
                (value
                  (identifier))
                (value
                  (identifier))))))
        (return_statement
          (call_expr
            (member_expr
              (const_identifier)
              (identifier))
            (argument_list
              (value
                (identifier)))))))))

================================
Pure function declaration
================================

let add = pure (a: i32, b: i32) -> i32 => a + b

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (pure_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type))
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (binary_expr
        (identifier)
        (add_operator)
        (identifier)))))

================================
Public pure function
================================

pub let multiply = pure (a: i32, b: i32) -> i32 => a * b

---

(program
  (declaration
    (visibility)
    (identifier)
    (lambda_expr
      (pure_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type))
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (binary_expr
        (identifier)
        (mul_operator)
        (identifier)))))

================================
Pure async function
================================

let compute = pure async (n: i32) -> i32 => n * 2

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (pure_modifier)
      (async_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (binary_expr
        (identifier)
        (mul_operator)
        (integer_literal
          (decimal_int))))))

================================
Pure with pattern matching
================================

let fib = pure (n: i32) -> i32 {
  (0) => 0,
  (1) => 1,
  (n) => fib(n-1) + fib(n-2),
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (pure_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (lambda_clause_list
        (lambda_clause
          (pattern_parameter_list
            (literal_pattern
              (integer_literal
                (decimal_int))))
          (integer_literal
            (decimal_int)))
        (lambda_clause
          (pattern_parameter_list
            (literal_pattern
              (integer_literal
                (decimal_int))))
          (integer_literal
            (decimal_int)))
        (lambda_clause
          (pattern_parameter_list
            (identifier))
          (binary_expr
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))
            (add_operator)
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))))))))

==============================================================
Basic Function declaration without parameters with return type
==============================================================

let foo = () -> i32 => 42

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list)
      (return_type
        (signed_integer_type))
      (integer_literal
        (decimal_int)))))

==========================================
Basic Function declaration with parameters
==========================================

let sum = (a: i32, b: i32) -> i32 => a + b

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type))
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (binary_expr
        (identifier)
        (add_operator)
        (identifier)))))

==================================================
Basic Function declaration with generic parameters
==================================================

let sum<n> = (a: n, b: n) -> n => a + b

---

(program
  (declaration
    (identifier)
    (generic_parameters
      (generic_parameter
        (generic_type)))
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (generic_type))
        (parameter
          (identifier)
          (generic_type)))
      (return_type
        (generic_type))
      (binary_expr
        (identifier)
        (add_operator)
        (identifier)))))

===================================================================
Basic Function declaration with generic parameters with constraints
===================================================================

let compare<n> where n: Ord = (a: n, b: n) -> n => a <=> b

---

(program
  (declaration
    (identifier)
    (generic_parameters
      (generic_parameter
        (generic_type)))
    (generic_parameter_constraints
      (generic_parameter_constraint
        (generic_type)
        (generic_bounds
          (trait_name))))
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (generic_type))
        (parameter
          (identifier)
          (generic_type)))
      (return_type
        (generic_type))
      (boolean_expr
        (identifier)
        (spaceship_operator)
        (identifier)))))

=====================================
Function with patterns for parameters
=====================================

let foo = ({ x, y }: Point, [one, two, three, ...rest]: []string, (alpha, beta): SomeTuple) -> void => {
  // do stuff
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list
        (parameter
          (struct_pattern
            (struct_field_pattern
              (identifier))
            (struct_field_pattern
              (identifier)))
          (user_defined_type_name))
        (parameter
          (array_pattern
            (identifier)
            (identifier)
            (identifier)
            (rest_pattern
              (identifier)))
          (array_type
            (string_type)))
        (parameter
          (tuple_pattern
            (identifier)
            (identifier))
          (user_defined_type_name)))
      (return_type
        (void_type))
      (block
        (comment)))))

=====================
Higher order function
=====================

let map<t,u> = (array: []t, func: (t) -> u) -> []u => {
  //  new_array = []
  //  for item in array {
  //    new_array.push(func(item))
  //  }
  //  new_array
}

---

(program
  (declaration
    (identifier)
    (generic_parameters
      (generic_parameter
        (generic_type))
      (generic_parameter
        (generic_type)))
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (array_type
            (generic_type)))
        (parameter
          (identifier)
          (lambda_type
            (parameter_type_list
              (parameter_type
                (generic_type)))
            (generic_type))))
      (return_type
        (array_type
          (generic_type)))
      (block
        (comment)
        (comment)
        (comment)
        (comment)
        (comment)))))

==================================================
Recursive Function with multiple function patterns
==================================================

let fib = pure rec (n: i32) -> i32 {
  (0) => 0,
  (1) => 1,
  (n) => fib(n-2) + fib(n-1),
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (pure_modifier)
      (rec_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (lambda_clause_list
        (lambda_clause
          (pattern_parameter_list
            (literal_pattern
              (integer_literal
                (decimal_int))))
          (integer_literal
            (decimal_int)))
        (lambda_clause
          (pattern_parameter_list
            (literal_pattern
              (integer_literal
                (decimal_int))))
          (integer_literal
            (decimal_int)))
        (lambda_clause
          (pattern_parameter_list
            (identifier))
          (binary_expr
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))
            (add_operator)
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))))))))

==================================================
Function with multiple function patterns and guard
==================================================

let fib = pure (n: i32) -> i32 {
  (n) if n < 2 => n,
  (n) => fib(n-2) + fib(n-1),
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (pure_modifier)
      (parameter_list
        (parameter
          (identifier)
          (signed_integer_type)))
      (return_type
        (signed_integer_type))
      (lambda_clause_list
        (lambda_clause
          (pattern_parameter_list
            (identifier))
          (guard
            (boolean_expr
              (identifier)
              (less_than_operator)
              (integer_literal
                (decimal_int))))
          (identifier))
        (lambda_clause
          (pattern_parameter_list
            (identifier))
          (binary_expr
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))
            (add_operator)
            (call_expr
              (identifier)
              (argument_list
                (value
                  (binary_expr
                    (identifier)
                    (sub_operator)
                    (integer_literal
                      (decimal_int))))))))))))

=================================
Function type parameter modifiers
=================================

let render = (scene: ref Scene) -> void => {
  // render the scene
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (type_modifier)
          (user_defined_type_name)))
      (return_type
        (void_type))
      (block
        (comment)))))

=============================
Function type return modifier
=============================

let open_file = (path: string) -> mut File => {
  // do some stuff
  return file
}

---

(program
  (declaration
    (identifier)
    (lambda_expr
      (parameter_list
        (parameter
          (identifier)
          (string_type)))
      (return_type
        (type_modifier)
        (user_defined_type_name))
      (block
        (comment)
        (return_statement
          (identifier))))))

===================================
Function without body is an error
:error
===================================

let foo = () =>

---



=============================================
Missing comma in parameters is an error
:error
=============================================

let foo = (x: i32 y: i32) -> i32 => x * y

---



===================================
Unclosed generic params is an error
:error
===================================

let sum<n = (a: n, b: n) -> n => a + b

---



===========================
Pure keyword in wrong order
:error
===========================

let compute = async pure (n: i32) -> i32 => n * 2

---
