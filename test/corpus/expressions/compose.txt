===========================
Basic function composition
===========================

let transform = negate >> double

---

(program
  (declaration
    (identifier)
    (compose_expression
      (identifier)
      (compose_operator)
      (identifier))))

======================================
Right-associative chained composition
======================================

let pipeline = parse >> validate >> serialize

---

(program
  (declaration
    (identifier)
    (compose_expression
      (identifier)
      (compose_operator)
      (compose_expression
        (identifier)
        (compose_operator)
        (identifier)))))

===========================================
Composition with partially applied functions
===========================================

let f = add(1) >> mul(2)

---

(program
  (declaration
    (identifier)
    (compose_expression
      (call_expression
        (identifier)
        (argument_list
          (value
            (integer_literal
              (decimal_int)))))
      (compose_operator)
      (call_expression
        (identifier)
        (argument_list
          (value
            (integer_literal
              (decimal_int))))))))

================================================
Nested generic type >> does not conflict
================================================

let x: Gen<Maybe<i32>> = foo

---

(program
  (declaration
    (identifier)
    (type_annotation
      (parameterized_type
        (user_defined_type_name)
        (parameterized_type
          (user_defined_type_name)
          (signed_integer_type))))
    (identifier)))
